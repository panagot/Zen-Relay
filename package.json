{
  "name": "zenrelay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n relay,client -c yellow,blue \"npm run dev:relay\" \"npm run dev:client\"",
    "dev:relay": "cd relay && node server.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
